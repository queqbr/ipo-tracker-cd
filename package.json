{
  "name": "ipo-tracker",
  "private": true,
  "type": "module",
  "scripts": {
    "refresh": "node scripts/refresh.js",
    "serve": "python3 -m http.server 8000",
    "probe": "node scripts/probe.js"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
